# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Django / runtime
db.sqlite3
/media/
/staticfiles/

# Scolta build artifacts (regenerated by `manage.py scolta_build`)
/.scolta-state/
/pagefind_index/

# Tooling
.pytest_cache/
.ruff_cache/
# uv.lock pins the editable local path deps (../../packages); not reproducible
# standalone. The published build resolves scolta/scolta-django from PyPI.
uv.lock

# Secrets / local env (DDEV holds an Anthropic test key in config.local.yaml)
.env
.env.*
/.ddev/config.local.yaml
/.ddev/config.*.local.yaml

# Content is COMMITTED — it is the corpus the demo ships.
!/content/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
